{
  "name": "TEMP_webhook_apply_v2_4_qa_repair_migration",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "temp/apply-v2-4-qa-repair-migration",
        "responseMode": "lastNode",
        "authentication": "headerAuth"
      },
      "id": "migration-webhook",
      "name": "Migration Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "AI_AGENT_WEBHOOK_AUTH",
          "name": "AI_AGENT_WEBHOOK_AUTH"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Non-breaking v2.4 QA repair attempt tracking.\n\nCREATE TABLE IF NOT EXISTS content_repair_attempts (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  job_id UUID NOT NULL REFERENCES content_jobs(id) ON DELETE CASCADE,\n  output_id UUID REFERENCES content_outputs(id) ON DELETE SET NULL,\n  repair_type TEXT NOT NULL,\n  issue_code TEXT NOT NULL,\n  status TEXT NOT NULL DEFAULT 'queued',\n  attempt_number INT NOT NULL DEFAULT 1,\n  max_attempts INT NOT NULL DEFAULT 2,\n  input_payload JSONB NOT NULL DEFAULT '{}'::jsonb,\n  output_payload JSONB NOT NULL DEFAULT '{}'::jsonb,\n  error_message TEXT,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  completed_at TIMESTAMPTZ\n);\n\nCREATE INDEX IF NOT EXISTS idx_repair_attempts_job_status ON content_repair_attempts(job_id, status);\n\nSELECT true AS migration_applied, 'v2.4_qa_repair_attempts' AS migration;"
      },
      "id": "apply-migration",
      "name": "Apply v2.4 QA Repair Migration",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 1,
      "position": [
        260,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "POSTGRES_AI_CONTENT_DB",
          "name": "POSTGRES_AI_CONTENT_DB"
        }
      }
    },
    {
      "parameters": {
        "responseBody": "={{$json}}",
        "options": {}
      },
      "id": "respond",
      "name": "Return Migration Result",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        520,
        0
      ]
    }
  ],
  "connections": {
    "Migration Webhook": {
      "main": [
        [
          {
            "node": "Apply v2.4 QA Repair Migration",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apply v2.4 QA Repair Migration": {
      "main": [
        [
          {
            "node": "Return Migration Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": false,
  "id": "TEMP_webhook_apply_v2_4_qa_repair_migration"
}
